package com.telran;

public class Main2 {

    public static void main(String[] args) {


        //"принимает A, возвращает B"
        //Function (x) -> x. | BiFunction (a, b) ->
        //.map | .flatMap

        //"создает из ничего" (ничего не принимает, но возвращает)
        //Supplier (поставщик)
        //.generate( () -> { return new Person() | результат вычисления | чтения из файла}  )

        //"взял деньги и ничего не вернул обратно"
        //Consumer
        //.peek() | void

        // п1: -стол
        // предикат: -да
        // п2: -оранжевый
        // предикат: -нет
        //Predicate
        //.filter()

        //"что взял, то и вернул"
        //Operator (same value)
        //.reduce ?
    }
}


